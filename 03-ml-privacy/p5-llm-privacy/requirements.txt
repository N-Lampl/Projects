# Fallback if you're not using uv/pyproject. Install with:
#   python3 -m pip install -r requirements.txt
torch>=2.2,<2.5
numpy>=1.24
matplotlib>=3.7
# dev
pytest>=8
ruff>=0.5
# OPTIONAL enhanced path (real GPT-2 exposure test, scripts/gpt2_exposure.py):
#   pip install "transformers>=4.40"
# transformers>=4.40
