# Fallback if you're not using uv/pyproject. Install with:
#   python3 -m pip install -r requirements.txt
# DEFAULT path (offline, no downloads, no Opacus):
torch>=2.2,<2.5
numpy>=1.24
scikit-learn>=1.3
matplotlib>=3.7
# dev
pytest>=8
ruff>=0.5
# --- OPTIONAL enhanced paths (NOT needed for `make run`) ---
# Reference DP-SGD + RDP accountant to validate the manual implementation:
#   opacus>=1.5            # github.com/meta-pytorch/opacus (verified 2026)
# Real-data tabular pool:
#   torchvision>=0.17      # Fashion-MNIST, downloaded on first use
