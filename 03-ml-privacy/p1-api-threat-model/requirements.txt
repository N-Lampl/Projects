# Default (offline) path — these are enough for `make demo` and `make test`.
scikit-learn==1.5.1
numpy==1.26.4
pandas==2.2.2
matplotlib==3.9.1

# Test/lint tooling
pytest==8.3.2
ruff==0.6.2

# OPTIONAL enhanced transport — only needed for `make serve-fastapi` / the @slow test.
# Verified-current (2026) pins; install with: pip install -r requirements.txt
fastapi==0.111.0
uvicorn==0.30.6
httpx==0.27.0
