# models/ (git-ignored)

The target model and all shadow models are trained **in-memory** at attack time
(they are tiny and CPU-fast), so nothing is persisted here by default. This folder
exists so the layout matches the rest of the portfolio; any checkpoints you choose
to save would land here and are git-ignored.
