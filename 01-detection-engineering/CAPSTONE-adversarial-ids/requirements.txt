# Fallback if you're not using uv/pyproject. Install with:
#   python3 -m pip install -r requirements.txt
# --- default offline path (these are enough to run `make attack` + tests) ---
scikit-learn>=1.3
numpy>=1.24
pandas>=2.0
matplotlib>=3.7
# --- OPTIONAL enhanced path (not needed for the default run) ---
# adversarial-robustness-toolbox==1.20.1   # IBM ART; enables `make attack ARGS=--use-art`
# --- dev ---
pytest>=8
ruff>=0.5
