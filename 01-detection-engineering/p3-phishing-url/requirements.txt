# Fallback if you're not using uv/pyproject. Install with:
#   python3 -m pip install -r requirements.txt
# --- default (offline) path: synthetic URLs + lexical features + sklearn ---
scikit-learn>=1.3
numpy>=1.24
pandas>=2.0
matplotlib>=3.7
# dev
pytest>=8
ruff>=0.5
# --- OPTIONAL enhanced paths (not needed for the default run) ---
# real PhiUSIIL dataset:
#   ucimlrepo>=0.0.7
# torch char-CNN (`make detect ARGS="--model cnn"`):
#   torch>=2.2,<2.5
