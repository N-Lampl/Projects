# Fallback if you're not using uv/pyproject. Install with:
#   python3 -m pip install -r requirements.txt
# --- default offline path (these are enough to run `make run` + tests) ---
scikit-learn>=1.3
numpy>=1.24
pandas>=2.0
matplotlib>=3.7
# --- OPTIONAL enhanced path (not needed for the default run) ---
# xgboost>=2.0          # gradient-boosted-tree classifier (build_pipeline(..., classifier="xgb"))
# --- dev ---
pytest>=8
ruff>=0.5
