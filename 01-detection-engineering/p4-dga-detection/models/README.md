# models/ (git-ignored)

Trained detectors are small and cheap to reproduce, so nothing here is committed.
Re-create everything with `make detect` (sklearn) or `make lstm` (char-LSTM).
The detector trains in a few seconds on CPU.
