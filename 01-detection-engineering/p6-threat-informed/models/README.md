# models/ (git-ignored)

This project trains no model — it is rule-based detection engineering. This folder exists only to
match the portfolio's standard layout and is intentionally empty (and git-ignored).
