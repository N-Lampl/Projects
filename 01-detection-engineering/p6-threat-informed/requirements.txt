# Fallback if you're not using uv/pyproject. Install with:
#   python3 -m pip install -r requirements.txt
numpy>=1.24
matplotlib>=3.7
# dev
pytest>=8
ruff>=0.5
# OPTIONAL enhanced path (loader falls back to a built-in YAML parser without it)
pyyaml>=6
