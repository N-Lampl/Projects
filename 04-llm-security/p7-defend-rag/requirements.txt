# Fallback if you're not using uv/pyproject. Install with:
#   python3 -m pip install -r requirements.txt
scikit-learn>=1.3
numpy>=1.24
matplotlib>=3.7
joblib>=1.3
torch>=2.2,<2.5
# dev
pytest>=8
ruff>=0.5
# --- OPTIONAL enhanced path (NOT needed for the default offline `make defend`) ---
# Declarative input/output rails (NVIDIA-NeMo/Guardrails):
#   nemoguardrails>=0.15
# Real LLM providers for the p4 target (use your OWN keys; see ../../ETHICS.md):
#   openai>=1.0
#   anthropic>=0.40
# Ollama uses the local HTTP API only (no pip dependency).
