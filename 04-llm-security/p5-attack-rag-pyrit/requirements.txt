# Default offline path needs only these (all installed in the base env):
numpy>=1.24
torch>=2.2,<2.5
matplotlib>=3.7
# dev
pytest>=8
ruff>=0.5
# OPTIONAL real red-team path (your own API key / local Ollama):
#   pip install pyrit-redteam==0.14.*   # Microsoft PyRIT v0.14.x
#   pip install garak                   # NVIDIA garak v0.15.x (see ../p2-garak-scan)
