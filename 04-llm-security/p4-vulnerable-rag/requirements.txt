# Fallback if you're not using uv/pyproject. Install with:
#   python3 -m pip install -r requirements.txt
scikit-learn>=1.3
numpy>=1.24
matplotlib>=3.7
# dev
pytest>=8
ruff>=0.5
# --- OPTIONAL enhanced paths (NOT needed for the default offline `make run`) ---
# Dense retriever instead of TF-IDF:
#   sentence-transformers>=2.2
# Real LLM providers (use your OWN keys; see ../../ETHICS.md):
#   openai>=1.0
#   anthropic>=0.40
# Ollama uses the local HTTP API only (no pip dependency).
