# Fallback if you're not using uv/pyproject. Install with:
#   python3 -m pip install -r requirements.txt
# --- default offline path (all that `make run` needs) ---
torch>=2.2,<2.5
numpy>=1.24
matplotlib>=3.7
scikit-learn>=1.3
# dev
pytest>=8
ruff>=0.5
# --- REAL path (`make abliterate`): extract from a real open-weight instruct model ---
# (downloads ~1GB weights; runs on CPU. Not needed for the offline `make run`.)
transformers>=4.44,<4.49
accelerate>=0.30
huggingface_hub>=0.24
