# Fallback if you're not using uv/pyproject. Install with:
#   python3 -m pip install -r requirements.txt
# --- default offline path (all that `make run` needs) ---
torch>=2.2,<2.5
numpy>=1.24
matplotlib>=3.7
scikit-learn>=1.3
# dev
pytest>=8
ruff>=0.5
# --- OPTIONAL enhanced path: extract from a real open-weight instruct model ---
# (downloads weights; GPU-preferred. Not needed for the default run.)
# transformers>=4.44
# accelerate>=0.30
