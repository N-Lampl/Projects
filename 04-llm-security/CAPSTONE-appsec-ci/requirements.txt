# DEFAULT OFFLINE path needs only these (already in the portfolio base env):
#   python3 -m pip install -r requirements.txt
numpy>=1.24
matplotlib>=3.7
# dev
pytest>=8
ruff>=0.5
#
# The pipeline REUSES the offline harnesses in the sibling projects
# (../p2-garak-scan, ../p3-promptfoo-redteam) and the target in
# ../p4-vulnerable-rag / ../p7-defend-rag. Those are pure-python and need nothing
# extra; if a sibling is missing, built-in fallbacks keep the gate running.
#
# --- OPTIONAL enhanced paths (NOT needed for `make run` / the CI gate) ---------
# Swap the offline harnesses for the real tools against YOUR OWN local model:
#   pip install "garak>=0.15,<0.16"          # real garak scan (Ollama/API target)
#   npx promptfoo@latest redteam run         # promptfoo owasp:llm preset (Node>=18)
#   pip install "pyrit-ai==0.14.*"           # PyRIT orchestrators (see p5)
#   nemoguardrails (github.com/NVIDIA-NeMo/Guardrails)  # runtime guardrails (p7)
