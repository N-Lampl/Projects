# models/ (git-ignored)

Trained weights are written here by `make train` / `make run`:

- `standard.pt` — the standard-trained SmallCNN (the vulnerable baseline).
- `adversarial.pt` — the PGD-adversarially-trained SmallCNN (the robust model).

Both are produced at runtime and are not committed.
