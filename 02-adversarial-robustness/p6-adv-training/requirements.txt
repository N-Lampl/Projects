# Fallback if you're not using uv/pyproject. Install with:
#   python3 -m pip install -r requirements.txt
# DEFAULT offline path needs only these:
torch>=2.2,<2.6
numpy>=1.24
matplotlib>=3.7
# dev
pytest>=8
ruff>=0.5
# OPTIONAL enhanced path (real MNIST instead of synthetic): make run ARGS=--real
# torchvision>=0.17,<0.21
