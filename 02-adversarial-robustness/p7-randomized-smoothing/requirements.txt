# Fallback if you're not using uv/pyproject. Install with:
#   python3 -m pip install -r requirements.txt
torch>=2.2,<2.5
numpy>=1.24
matplotlib>=3.7
# --- optional enhanced paths (the default run works WITHOUT these) ---
scipy>=1.10        # exact Clopper-Pearson / normal quantiles (pure-numpy fallback otherwise)
torchvision>=0.17,<0.20   # real MNIST via --dataset mnist (default is synthetic)
# dev
pytest>=8
ruff>=0.5
