# Pinned fallback deps for the DEFAULT (offline, synthetic-data) path.
torch==2.2.2
numpy==1.26.4
matplotlib==3.8.4

# --- OPTIONAL enhanced path (uncomment to enable) ---
# real MNIST instead of synthetic glyphs (run scripts with --real):
# torchvision==0.17.2
#
# These attacks are HAND-ROLLED here; if you want to cross-check against the
# reference implementations, install one of:
# adversarial-robustness-toolbox==1.20.1
# torchattacks==3.5.1
# foolbox==3.3.4
