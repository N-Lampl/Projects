# models/ (git-ignored)

- `smallcnn_synth.pt` — the offline SmallCNN target, written by `make attack` on
  first run (self-trained on synthetic data). Not committed.
- The optional pretrained ResNet-18 weights are cached by torchvision in your
  `~/.cache/torch/hub/checkpoints/`, not here.
