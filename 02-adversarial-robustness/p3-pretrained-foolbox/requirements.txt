# Fallback if you're not using uv/pyproject. Install with:
#   python3 -m pip install -r requirements.txt
torch>=2.2,<2.5
torchvision>=0.17,<0.20
numpy>=1.24
matplotlib>=3.7
# dev
pytest>=8
ruff>=0.5
# OPTIONAL enhanced path (cross-check the hand-rolled attack). Not needed for `make attack`:
# foolbox==3.5.1
