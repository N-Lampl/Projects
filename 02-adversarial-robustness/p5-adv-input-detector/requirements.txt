# Fallback if you're not using uv/pyproject. Install with:
#   python3 -m pip install -r requirements.txt
# DEFAULT (offline) path needs only these:
torch>=2.2,<2.5
scikit-learn>=1.3
numpy>=1.24
matplotlib>=3.7
# OPTIONAL enhanced path: real MNIST instead of synthetic digits
#   run: make detect ARGS="--dataset mnist"
torchvision>=0.17,<0.20
# dev
pytest>=8
ruff>=0.5
