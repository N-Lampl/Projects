# Fallback if you're not using uv/pyproject. Install with:
#   python3 -m pip install -r requirements.txt
torch>=2.2,<2.6
torchvision>=0.17,<0.21
numpy>=1.24
matplotlib>=3.7
# dev
pytest>=8
ruff>=0.5
# OPTIONAL enhanced path (NOT needed for the default synthetic run):
#   reference attack library to cross-check the from-scratch implementations.
# torchattacks==3.5.1
