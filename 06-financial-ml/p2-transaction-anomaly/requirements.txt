# Fallback if you're not using uv/pyproject. Install with:
#   python3 -m pip install -r requirements.txt
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
matplotlib>=3.7
# optional: the autoencoder detector (run with `make detect ARGS=--ae`)
# torch>=2.2,<2.5
# dev
pytest>=8
ruff>=0.5
