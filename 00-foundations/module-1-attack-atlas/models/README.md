# models/ (git-ignored)

This project trains no models — it is a documentation/threat-modeling artifact
(ATLAS x ATT&CK mapping). This folder exists only to match the portfolio's
standard project layout and is intentionally empty.
