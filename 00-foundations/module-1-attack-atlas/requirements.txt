# The DEFAULT build path is stdlib-only — nothing here is needed to run it.
# These are only for development (linting + tests).
#   python3 -m pip install -r requirements.txt
pytest>=8
ruff>=0.5

# OPTIONAL enhanced path (not used by the default build; documented in README):
#   - mitreattack-python  -> pull the live ATT&CK STIX bundle to validate IDs
#     against the current MITRE catalog instead of the curated subset.
# mitreattack-python>=3.0
