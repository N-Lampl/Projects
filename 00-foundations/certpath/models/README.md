# models/ (git-ignored)

This is a study-plan artifact — it trains **no models** and saves **no weights**. This folder exists
only to keep the repo's per-project skeleton uniform (`data/`, `models/`, `results/`). Nothing is
written here at runtime.
