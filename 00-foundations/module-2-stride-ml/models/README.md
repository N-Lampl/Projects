# models/ (git-ignored)

No trained weights are produced by this project — it is a threat model, not a
training pipeline. This folder is kept only to mirror the repo's standard layout.
The ML inference service we analyze *would* load a signed model artifact from a
registry (threat **ML01**), but that artifact is out of scope here.
