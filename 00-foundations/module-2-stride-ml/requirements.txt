# Fallback if you're not using uv/pyproject. Install with:
#   python3 -m pip install -r requirements.txt
# The DEFAULT run path is stdlib-only; matplotlib is used only for the bar chart.
matplotlib>=3.7
# optional enhanced path (verified 2026): pytm threat-modeling-as-code
# pytm>=1.3
# dev
pytest>=8
ruff>=0.5
