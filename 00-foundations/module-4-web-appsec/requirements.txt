# Fallback if you're not using uv/pyproject. Install with:
#   python3 -m pip install -r requirements.txt
#
# DEFAULT (offline) path needs only matplotlib + the Python stdlib.
matplotlib>=3.7
# dev
pytest>=8
ruff>=0.5
# OPTIONAL enhanced/live path (against the docker container). Not needed for `make lab`:
#   requests>=2.31
