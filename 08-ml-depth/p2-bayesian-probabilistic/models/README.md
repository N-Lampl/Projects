# Models

No model weights are produced or committed here (`models/` is git-ignored). The
"model" is a hierarchical Bayesian posterior sampled **in memory** by the numpy
Gibbs sampler at run time — there is nothing to persist. This folder exists only to
keep the project skeleton identical to the rest of the monorepo.
