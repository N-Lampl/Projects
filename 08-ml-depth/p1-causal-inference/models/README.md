# Models

No model weights are produced or committed here (`models/` is git-ignored). The
estimators fit lightweight scikit-learn models (linear/logistic regressions) **in
memory** at run time — there is nothing to persist. This folder exists only to keep
the project skeleton identical to the rest of the monorepo.
